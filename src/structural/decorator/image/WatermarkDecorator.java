package structural.decorator.image;

public class WatermarkDecorator extends ImageDecorator {
    public WatermarkDecorator(ImageProcess imageProcess, String value) {
        super(imageProcess, value);

    }
}
